# Word Font Converter
A simple python script to convert word documents (either .doc or .dox) between a large font type and a standard font type.

## Dependencies
Python (tested down to version 3.8.10)
pywin32 (`pip install pywin32`)
